#!/bin/bash

# Count the number of directories
count=$(find . -mindepth 1 -maxdepth 1 -type d -name ".*" -o -type d | wc -l)

echo "The current directory contains $count directories (excluding current and parent directories)."

